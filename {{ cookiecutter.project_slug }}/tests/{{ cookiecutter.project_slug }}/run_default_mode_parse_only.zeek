# This test ensures that the script can be parsed by Zeek. An error here means some sort of syntax error.

# @TEST-EXEC: zeek --parse-only {{ cookiecutter.project_namespace }}/{{ cookiecutter.project_slug }}
