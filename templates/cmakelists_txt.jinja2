# Generated by binpac_quickstart

include(ZeekPlugin)

include_directories(BEFORE ${CMAKE_CURRENT_SOURCE_DIR} ${CMAKE_CURRENT_BINARY_DIR})

zeek_plugin_begin({{space}} {{ name|upper }})
	zeek_plugin_cc({{ name|upper }}.cc Plugin.cc)
	zeek_plugin_bif(events.bif)
	zeek_plugin_pac({{ name|lower }}.pac {{ name|lower }}-analyzer.pac {{ name|lower }}-protocol.pac)
zeek_plugin_end()
