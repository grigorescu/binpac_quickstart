# Generated by binpac_quickstart

include(BroPlugin)

include_directories(BEFORE ${CMAKE_CURRENT_SOURCE_DIR} ${CMAKE_CURRENT_BINARY_DIR})

bro_plugin_begin({{space}} {{ name|upper }})
	bro_plugin_cc({{ name|upper }}.cc Plugin.cc)
	bro_plugin_bif(events.bif)
	bro_plugin_pac({{ name|lower }}.pac {{ name|lower }}-analyzer.pac {{ name|lower }}-protocol.pac)
bro_plugin_end()
