# Generated by binpac_quickstart

project(Zeek-Plugin-{{name|upper}})

cmake_minimum_required(VERSION 2.8)

if ( NOT BRO_DIST )
    message(FATAL_ERROR "BRO_DIST not set")
endif ()

set(CMAKE_MODULE_PATH ${BRO_DIST}/cmake)

include(ZeekPlugin)

zeek_plugin_begin(Bro {{ name|upper }})
zeek_plugin_cc(src/Plugin.cc)
zeek_plugin_cc(src/{{ name|upper }}.cc)
zeek_plugin_bif(src/events.bif)
zeek_plugin_pac(src/{{ name|lower }}.pac src/{{ name|lower }}-analyzer.pac src/{{ name|lower }}-protocol.pac)
zeek_plugin_end()
