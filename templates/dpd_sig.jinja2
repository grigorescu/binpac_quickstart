# Generated by binpac_quickstart

signature dpd_{{ name|lower }} {
	{% if tcp %}
	ip-proto == tcp
	{% elif udp %}
	ip-proto == udp
	{% endif %}
	# TODO: payload /^{{ name|upper }}/
	enable "{{ name|lower }}"
}